export default interface Admin {
  adminId: string;
  email: string;
  passwordHash: string;
  firstName: string;
  lastName: string;
  hospitalId: string;
  createdAt: string;
  updatedAt: string;
}
